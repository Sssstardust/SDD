# SDD (Spec-Driven Development) 流程实战体验与技术优化白皮书

作为深度参与 SDD 流程并完成全周期开发（如 VRM 产品信息查询接口）的智能架构师，结合实战感受，对 SDD 流程的现状、不足及改进建议总结如下：

## 🛡️ 核心价值感受
SDD 最强大的地方在于**“事实驱动的治理”**。它强迫开发者（或 AI）在写代码前先思考“存量事实”（`module-map`）和“契约约束”（`OpenAPI/SQL`）。这不仅避免了随意编码，更通过 Gate 机制确保了需求到代码的 **100% 追溯性**。

---

## ❌ 当前流程的不足点

### 1. 需求抽取的“逻辑浅表化”与“信息损耗”
- **表现**：生成的 `feature-brief.md` 往往只是 PRD 文本的简单结构化，缺乏对实现路径（类、方法、调用链）的预建模。
- **后果**：导致后续生成的任务切片（Slice）信息熵不足，被迫退避为“填充 TODO”的空模板。

### 2. “任务管理”与“流程流水线”的逻辑断层
- **表现**：`shrimp-task-manager` 的“已完成”状态无法自动触发 `sdd-pipeline` 的 Gate 校验。
- **后果**：开发者容易在编码结束后忘记执行基线同步，导致新代码成为“孤岛资产”，无法被系统感知。

### 3. 环境兼容性的“机械摩擦” (Windows 特有)
- **表现**：Pipeline 脚本在执行 `os.replace` 或更新 JSON 报告时，频繁因文件被 IDE 或其他进程锁定而崩溃。
- **后果**：自动化流程中断，迫使开发者手动干预数据，破坏了流程的严肃性和连贯性。

### 4. Gate 5 (覆盖率验证) 的“刚性过载”
- **表现**：要求生成的验证类必须 100% 运行且移除 TODO 才能 PASS。
- **后果**：在 Brownfield 环境下，存量依赖缺失常导致验证类不可编译，形成“代码已写好但流程过不去”的死循环。

---

## 💡 深度细化改进方案

### 1. 强化 Feature Brief 的“实现预演”机制 (Skill + Script 协同)
**解决目标**：彻底消除 Slice 模板化，确保“任务即工单”。

*   **Skill 优化 (大脑)**：升级 `requirement-analyzer` 技能。
    - **新增指令**：强制执行“逻辑原子建模”。Brief 必须包含一个 `logic_atoms` YAML 块。
    - **逻辑要求**：针对每个 REQ，明确其涉及的 `component` (类名)、`method` (方法名) 和 `sequence` (核心逻辑步骤)。
*   **Python 脚本优化 (骨架)**：升级 `generate_task_slices.py`。
    - **结构化映射**：脚本应优先解析 `logic_atoms`。如果 Brief 中已有方法名和逻辑步骤，脚本应将其直接注入 Slice 的 `implementationGuide` 字段，**强制覆盖默认模板**。
    - **产物示例**：生成的 `slice-xxx.md` 将自带“1. 在 X 类实现 Y 方法；2. 执行 A->B 逻辑”的精确指导。

### 2. 推行“产物驱动”的零冗余模式
**解决目标**：消除设计 SQL 与数据库任务之间的重复劳动。

*   **策略调整**：修改任务切片生成逻辑。
*   **具体实现**：对于 `db-change` 类型的切片，脚本自动检索 `design-pack/` 下的 SQL 脚本。
    - **任务定义**：切片内容不再是“手动建表”，而是定义为 **“执行并验证 SQL 资产”**。
    - **自动化链接**：切片中自动包含 SQL 的文件路径和摘要。开发者（或 AI）的任务是执行 `run_pipeline.py apply-db-changes` 并核对 schema 事实。

### 3. 建立“影子写入”策略解决文件冲突 (Windows 基础设施优化)
**解决目标**：消除权限锁定导致的 Pipeline 中断。

*   **实现路径**：优化 Pipeline 的 IO 核心。
    - **非侵入式更新**：在更新 `verify-report.json` 等关键文件时，先写入 `*.tmp` 影子文件。
    - **智能重试机制**：使用 `retry` 装饰器，配合 `powershell` 命令检测文件句柄占用。若锁定，延迟 500ms 后再次尝试。
    - **批处理同步**：允许 Pipeline 在内存中维护状态，待所有子流程结束后，通过单一进程一次性安全刷盘。

### 4. 引入“柔性 Gate”与“人工审计豁免”机制
**解决目标**：打破 Gate 5 验证失败导致的流程死循环。

*   **机制创新**：引入 `waived_violations`（人工豁免）协议。
*   **具体方案**：当自动化验证类由于环境问题无法运行时，允许 AI 或开发者提交 **“逻辑证据链”**（如关键源码片段、人工测试截图说明）。
    - **审计通过**：在 `gate-report.json` 中记录豁免原因和证据指纹，由 Pipeline 判定为“有条件 PASS”。
    - **效果**：确保治理流程能够灵活适应复杂的存量工程事实，而非僵化的“非黑即白”。

### 5. 建立“任务-流程”状态强同步钩子 (State Sync)
**解决目标**：确保实现与治理的步调一致。

*   **联动设计**：在 `shrimp-task-manager` 侧增加后置处理器。
*   **具体流程**：
    - 当任务列表中最后一个 `slice` 标记为 `verify_task` 完成时。
    - 底层自动触发 `sdd-pipeline flow-status`。
    - 若发现 `implementation_result` 非 `PASS`，则通过 CLI 弹窗或日志红色警告，阻止开发者在未基线同步的情况下宣称“已交付”。

---

## 🏁 愿景：从“审计员”进化为“导航仪”
SDD 的终极形态应该是：**流程隐藏在编码任务背后**。开发者只需要关注“如何解决问题”，而 Pipeline 像导航仪一样，在后台自动完成事实采集、冲突检测和基线同步。

**本次总结的技术方案将作为 SDD 流程演进的基石，致力于实现“写完代码即治理完成”的无感闭环。**
