# governance-warn-gate5-p1

用途：

- Gate 5 `WARN` 样例。
- 演示仅存在 `P1` 缺口时 Gate 5 不阻断，但会输出 `WARN` 并写入 `verify-report.json`。

命令链由 `python scripts/validate_fixture_matrix.py --fixture governance-warn-gate5-p1` 自动执行。
