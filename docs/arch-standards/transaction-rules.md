# 事务边界规范

## 1. 规则定位

用于约束涉及状态流转、数据库变更、支付链路时的事务边界定义，避免设计阶段遗漏一致性约束。

## 2. 必须项

- 关键状态切换必须明确事务边界
- 本地事务内要覆盖关键记录写入和核心状态更新
- 涉及数据库变更时必须有回滚策略
- 事务外部的异步、外部调用或补偿动作必须显式说明

## 3. 禁止项

- 只写“事务已处理”但不说明边界
- 在事务内无约束地串联长耗时外部调用
- 数据库变更脚本没有回滚路径

## 4. 审阅关注点

- 事务边界是否落到设计文档或关键策略文件
- 事务内外的动作划分是否清楚
- 回滚和补偿是否形成闭环
