# 接口文档

## 1. 元信息

- feature_name: tob-oa-office-demo
- design_version: design-v1.md
- req_ids: REQ-001、REQ-002、REQ-003、REQ-004、REQ-005、REQ-006、REQ-007、REQ-008
- 调用方: tob-oa-office-demo 上游调用方
- 被调用方: TobOaOfficeDemo 所属服务

## 2. 接口清单

| 接口名 | 方法 | 路径 | 用途 | 对应 REQ-ID |
| --- | --- | --- | --- | --- |
| gEmployees | GET | /api/v1/hr/employees | GET employees | REQ-001、REQ-002、REQ-003、REQ-004、REQ-005、REQ-006、REQ-007、REQ-008 |
| pEmployees | POST | /api/v1/hr/employees | POST employees | REQ-001、REQ-002、REQ-003、REQ-004、REQ-005、REQ-006、REQ-007、REQ-008 |
| gOrgTree | GET | /api/v1/hr/org-tree | GET org tree | REQ-001、REQ-002、REQ-003、REQ-004、REQ-005、REQ-006、REQ-007、REQ-008 |
| gApprovals | GET | /api/v1/approvals | GET approvals | REQ-001、REQ-002、REQ-003、REQ-004、REQ-005、REQ-006、REQ-007、REQ-008 |
| pLeave | POST | /api/v1/approvals/leave | POST leave | REQ-001、REQ-002、REQ-003、REQ-004、REQ-005、REQ-006、REQ-007、REQ-008 |
| pOvertime | POST | /api/v1/approvals/overtime | POST overtime | REQ-001、REQ-002、REQ-003、REQ-004、REQ-005、REQ-006、REQ-007、REQ-008 |
| pReimburse | POST | /api/v1/approvals/reimburse | POST reimburse | REQ-001、REQ-002、REQ-003、REQ-004、REQ-005、REQ-006、REQ-007、REQ-008 |
| pProcessDefinitions | POST | /api/v1/approvals/process-definitions | POST process definitions | REQ-001、REQ-002、REQ-003、REQ-004、REQ-005、REQ-006、REQ-007、REQ-008 |
| gNotices | GET | /api/v1/notices | GET notices | REQ-001、REQ-002、REQ-003、REQ-004、REQ-005、REQ-006、REQ-007、REQ-008 |
| pNotices | POST | /api/v1/notices | POST notices | REQ-001、REQ-002、REQ-003、REQ-004、REQ-005、REQ-006、REQ-007、REQ-008 |
| pPush | POST | /api/v1/messages/push | POST push | REQ-001、REQ-002、REQ-003、REQ-004、REQ-005、REQ-006、REQ-007、REQ-008 |
| gRecords | GET | /api/v1/attendance/records | GET records | REQ-001、REQ-002、REQ-003、REQ-004、REQ-005、REQ-006、REQ-007、REQ-008 |

## 3. 业务说明

- 该接口解决什么业务问题：人事：员工档案、组织架构
- 触发时机：用户或上游系统触发本功能的主流程时
- 前置条件：REQ- 范围内的输入条件满足且权限校验通过
- 后置结果：返回主流程处理结果，并为后续设计包提供可追溯接口约束

## 4. 请求说明

| 字段 | 类型 | 必填 | 含义 | 来源 |
| --- | --- | --- | --- | --- |
| query | string | 否 | GET employees的核心输入 | 请求头/查询参数 |

## 5. 响应说明

| 字段 | 类型 | 含义 | 备注 |
| --- | --- | --- | --- |
| result | object | 业务处理结果 | 包含状态与关键字段 |

## 6. 错误码说明

| 错误码 | HTTP 状态 | 触发条件 | 处理建议 |
| --- | --- | --- | --- |
| TOB_OA_OFFICE_DEMO_INVALID_INPUT | 400 | 参数缺失或非法 | 检查请求参数后重试 |
| TOB_OA_OFFICE_DEMO_CONFLICT | 409 | 状态冲突或重复提交 | 刷新状态后重试 |

## 7. 依赖与时序说明

- 是否依赖其他服务：否
- 是否涉及异步回调：是
- 是否需要幂等保护：否
- 关键依赖：无明确外部依赖

## 8. 人工审阅关注点

- 接口命名是否清晰：需在评审中确认
- 路径设计是否与现有接口冲突：需结合 baseline 扫描结果确认
- 错误码是否和全局规范一致：需在评审中核对
