# 数据模型

## 1. 元信息

- feature_name: tob-oa-office-demo
- design_version: design-v5.md
- req_ids: REQ-001、REQ-002、REQ-003、REQ-004、REQ-005、REQ-006、REQ-007、REQ-008

## 2. 实体与表映射

| 实体 | 表名 | 用途 | 对应 REQ-ID |
| --- | --- | --- | --- |
| ToB | PENDING_TABLE_TOB | 新领域模式下承载 ToB 数据 | REQ-001, REQ-002, REQ-003 |
| 审批 | PENDING_TABLE_TOB_OA_OFFICE_DEMO | 新领域模式下承载 审批 数据 | REQ-001, REQ-002, REQ-003 |
| 通知 | PENDING_TABLE_TOB_OA_OFFICE_DEMO | 新领域模式下承载 通知 数据 | REQ-001, REQ-002, REQ-003 |

## 3. 字段清单

### 3.1 主表字段

| 字段名 | 类型 | 含义 | 是否必填 | 默认值 | 备注 |
| --- | --- | --- | --- | --- | --- |
| id | varchar(64) | 支撑 tob-oa-office-demo 的 id 信息 | 是 | 无 |  |
| status | varchar(32) | 支撑 tob-oa-office-demo 的 status 信息 | 是 | DRAFT |  |
| created_at | datetime | 支撑 tob-oa-office-demo 的 created_at 信息 | 否 | 无 |  |
| updated_at | datetime | 支撑 tob-oa-office-demo 的 updated_at 信息 | 否 | 无 |  |

### 3.2 索引设计

| 索引名 | 字段 | 类型 | 目的 |
| --- | --- | --- | --- |
| idx_tob | status | 普通索引 | 支撑查询与状态过滤 |
| idx_entity | status | 普通索引 | 支撑查询与状态过滤 |
| idx_entity | status | 普通索引 | 支撑查询与状态过滤 |

## 4. 关系与约束

- 主外键关系：由主实体到扩展实体按业务关系建立
- 唯一约束：关键业务主键与状态组合需保证一致性
- 幂等约束：当前未额外声明幂等约束

## 5. 变更影响分析

- 对现有表的影响：当前处于待确认或新领域模式
- 对历史数据的影响：需评估历史数据回填与兼容策略
- 对查询性能的影响：索引设计需支撑主查询路径

## 6. 人工审阅关注点

- 字段语义是否清晰：需在评审中确认
- 索引是否过多或不足：需结合真实读写路径确认
- 是否存在模型冗余：需结合领域边界确认
