# 数据模型

## 1. 元信息

- feature_name: task-board
- design_version: design-v2.md
- req_ids: REQ-001、REQ-002、REQ-003、REQ-004、REQ-005

## 2. 实体与表映射

| 实体 | 表名 | 用途 | 对应 REQ-ID |
| --- | --- | --- | --- |
| Feature | t_order_pricing_relation | 支撑 Feature 相关需求 | REQ-001, REQ-002, REQ-003 |
| Brief | t_tob_pricing_item | 支撑 Brief 相关需求 | REQ-001, REQ-002, REQ-003 |
| Ready | t_tob_pricing_plan | 支撑 Ready 相关需求 | REQ-001, REQ-002, REQ-003 |

## 3. 字段清单

### 3.1 主表字段

| 字段名 | 类型 | 含义 | 是否必填 | 默认值 | 备注 |
| --- | --- | --- | --- | --- | --- |
| effective_time | datetime | 支撑 task-board 的 effective_time 信息 | 否 | 无 |  |
| pricing_id | varchar(64) | 支撑 task-board 的 pricing_id 信息 | 否 | 无 |  |
| pricing_name | varchar(128) | 支撑 task-board 的 pricing_name 信息 | 否 | 无 |  |
| pricing_type | varchar(128) | 支撑 task-board 的 pricing_type 信息 | 否 | 无 |  |
| product_line_code | varchar(128) | 支撑 task-board 的 product_line_code 信息 | 否 | 无 |  |
| status | varchar(32) | 支撑 task-board 的 status 信息 | 是 | DRAFT |  |

### 3.2 索引设计

| 索引名 | 字段 | 类型 | 目的 |
| --- | --- | --- | --- |
| idx_ready | pricing_name,product_line_code,status | 普通索引 | 支撑查询与状态过滤 |

## 4. 关系与约束

- 主外键关系：由主实体到扩展实体按业务关系建立
- 唯一约束：关键业务主键与状态组合需保证一致性
- 幂等约束：当前未额外声明幂等约束

## 5. 变更影响分析

- 对现有表的影响：可能涉及存量表字段/索引调整
- 对历史数据的影响：需评估历史数据回填与兼容策略
- 对查询性能的影响：索引设计需支撑主查询路径

## 6. 人工审阅关注点

- 字段语义是否清晰：需在评审中确认
- 索引是否过多或不足：需结合真实读写路径确认
- 是否存在模型冗余：需结合领域边界确认
